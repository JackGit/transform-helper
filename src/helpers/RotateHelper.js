import BaseHelper from './BaseHelper'

class RotateHelper extends BaseHelper {

  render () {
    // render Rotate related helpers: rotate handle, rotate origin point
  }
}

export default RotateHelper